{
  "name": "glowfic-transcript",
  "version": "0.1.0",
  "description": "Export a glowfic thread as a clean markdown or HTML transcript",
  "type": "module",
  "private": true,
  "scripts": {
    "pretest": "python3 build.py",
    "test": "node --test \"tests/**/*.test.js\"",
    "build": "python3 build.py"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
